# Dependencies
node_modules/

# Build output
dist/
.next/
*.tsbuildinfo

# Environment variables — never commit secrets
.env
.env.local
.env*.local

# User data (integration store, etc.) — keep the dir, ignore the data file
packages/web/data/*.json

# OS / editor
.DS_Store
Thumbs.db
.vscode/
*.swp
