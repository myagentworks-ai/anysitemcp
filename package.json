{
  "name": "webmcp",
  "private": true,
  "version": "0.1.0-alpha",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev:web": "pnpm --filter web dev"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
